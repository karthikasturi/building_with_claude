[project]
name = "secure-hello"
version = "0.1.0"
description = "Module 1 demo — secure client setup, key handling, and reading a response safely (based on day1/secure_call.py)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.117.0",
    "python-dotenv>=1.2.2",
]
